MITO.init({
  debug: true,
  // 默认20
  maxBreadcrumbs: 10,
  dsn: 'http://localhost:5000/errors/upload'
  // 默认false
  // enableTraceId: true,
  // 默认截取所有接口
  // filterXhrUrlRegExp: /\/likePoetry/
})
